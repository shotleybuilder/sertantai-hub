defmodule SertantaiHub.Repo.Migrations.AddOrganizationTier do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:organizations) do
      add :tier, :text, null: false, default: "blanket_bog"
    end
  end

  def down do
    alter table(:organizations) do
      remove :tier
    end
  end
end
